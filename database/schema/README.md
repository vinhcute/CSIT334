# Database Schema

The project uses Prisma as the typed database schema and access tool.

- Target database: PostgreSQL
- Prisma schema file: `backend/prisma/schema.prisma`
- Connection source: `DATABASE_URL` from the backend environment

Step 4 only configures the schema tool. Entity models are added in later Phase 01 steps after this empty schema validates successfully.
